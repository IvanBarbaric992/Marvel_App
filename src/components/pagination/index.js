import PaginationComponent from "./PaginationComponent";

export default PaginationComponent;

// we can export all components from one index file using export {default as PPaginationComponent } from "components/Pagination"
